#include <iostream>
#include <bits/stdc++.h>
using namespace std;

vector<int> solucion(vector<int> A, int K){
    return A;
}

int main(){
    vector<int> vect{1,2,3};
    vector<int> b;
    b = solucion(vect,1);
    for (int i=0;i<b.size();i++){
    cout<<b[i]<<endl;
    }
}